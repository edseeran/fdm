// export function isAuthenticated() {
//   const csrftoken = useCookie('csrftoken').value;  // Get csrf token using useCookie
//   const sessionid = useCookie('sessionid').value;  // Get session id using useCookie

//   // Check if both cookies are present
//   return !!csrftoken && !!sessionid;
// }
